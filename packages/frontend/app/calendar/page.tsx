/**
 * Race Calendar Page (/calendar)
 *
 * TODO:
 * - Fetch race schedule via getRaceSchedule(season)
 * - Support year switcher (query param ?season=2024)
 * - Render <RaceCalendarList /> component
 * - Link each race to /calendar/[round]
 */
export default function CalendarPage() {
  return (
    <main>
      <h1>Race Calendar</h1>
      <p>TODO: Season selector + list of races for current year</p>
    </main>
  );
}
