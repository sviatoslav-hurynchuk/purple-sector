/**
 * Dashboard Page (/)
 *
 * TODO:
 * - Fetch driver standings via getDriverStandings()
 * - Fetch constructor standings via getConstructorStandings()
 * - Fetch next race info
 * - Render <DriverStandingsTable /> component
 * - Render <ConstructorStandingsTable /> component
 * - Render <NextRaceCard /> component with countdown
 */
export default function DashboardPage() {
  return (
    <main>
      <h1>F1 Dashboard</h1>
      <p>TODO: Driver standings, constructor standings, next race card</p>
    </main>
  );
}
